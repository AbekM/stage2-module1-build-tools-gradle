repositories {
    mavenCentral()
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'utils'
    apply plugin: 'demo'

    dependencies {
        testImplementation('org.junit.jupiter:junit-jupiter:5.8.1')
    }
}